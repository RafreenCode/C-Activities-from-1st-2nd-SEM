#include <stdio.h>

int main(){

    // arithmetic operators
 
    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement
 
    int x = 5;
    int y = 2;
    int result = x + y;
    char pisteyawa[] = "I am God";
    float sum = (float) 3 / 2;
    
    // int z = x + y;
    // int z = x - y;
    // int z = x * y;
    // float z = x / (float) y;
    // int z = x % y;
    // x++;
    // y--;

    printf("%s\n", pisteyawa);
    printf("My answer is %d\n", result);
    printf("%d\n", y);
    printf("%.1f", sum);

    return 0;
}